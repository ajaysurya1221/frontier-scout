name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
          pip install detect-secrets
      - name: Lint platform
        run: make lint
      - name: Type check platform
        run: make type
      - name: Secret scan
        run: |
          detect-secrets scan --all-files --force-use-all-plugins \
            --exclude-files '\.git/|\.pytest_cache/|costs\.jsonl|quality-log\.jsonl|demo/' \
            > .secrets-scan-result.json
          python - <<'PY'
          import json, sys
          result = json.load(open(".secrets-scan-result.json"))
          findings = sum(len(v) for v in result.get("results", {}).values())
          print(f"detect-secrets found {findings} potential secrets")
          sys.exit(1 if findings else 0)
          PY
      - name: Compile Python
        run: python -m compileall scripts outputs tests frontier_scout
      - name: Focused coverage gate
        run: make coverage
      - name: Release preflight (CLI + deterministic demo + build)
        run: |
          frontier-scout --help > /dev/null
          frontier-scout demo
          git diff --exit-code -- demo/
          FRONTIER_SCOUT_HOME=.scratch/ci-home frontier-scout evaluate https://github.com/modelcontextprotocol/servers --json > /dev/null
          FRONTIER_SCOUT_HOME=.scratch/ci-home frontier-scout trial browser-use/browser-use --url https://github.com/browser-use/browser-use --dry-run --json > /dev/null
          FRONTIER_SCOUT_HOME=.scratch/ci-home frontier-scout guard --repo . --format github || true
          make eval
          make demo
          python -m build
      - name: Dependency audit
        run: make audit
      - name: Non-live test suite
        run: PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -m "not live" -q
