[pytest]
# Collect only the project's own suite. examples/sample-repo ships a demo test
# meant to run *inside* that sample repo, not as part of Frontier Scout's suite.
testpaths = tests
markers =
    live: tests that make real LLM API calls (require ANTHROPIC_API_KEY)
