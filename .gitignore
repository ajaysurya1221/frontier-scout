# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/
build/
dist/

# Secrets / local env
.env
.env.local
*.pem
*.key

# Editors / IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Frontier Scout runtime state lives in ~/.frontier-scout/ on the user's
# laptop. The repo should never contain a scan database or cost ledger.
*.db
*.db-journal
*.db-wal
*.db-shm
costs.jsonl
quality-log.jsonl
signals-log.jsonl

# Lab subprocess scratch workspaces (created under /tmp, but symlinks
# happen during local debugging).
ai-lab-experiments/
.scratch/

# Local screenshots / debugging artifacts (operator-only).
screenshots/
screenshots_*/
