# Core LLM client + caching support
anthropic>=0.45.0

# Source fetchers
feedparser>=6.0.11
requests>=2.32.0
beautifulsoup4>=4.12.0

# Deterministic policy gates around LLM output
pydantic>=2.0

# Round 10: HuggingFace runtime for the polyglot lab dispatcher. We
# deliberately do NOT depend on torch/tensorflow/jax — the HF lab loads
# config + tokenizer only and never runs inference, so the heavy ML
# frameworks would bloat the pipeline image without buying anything.
huggingface_hub>=0.24.0
transformers>=4.40.0

# Verdict quality CI (used in tests/, not at runtime)
pytest>=8.0.0
build>=1.2.0
