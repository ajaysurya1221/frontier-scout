# ADR 0004: Graph Store

Use SQLite graph tables/in-memory graph objects for the required path. Apache
AGE is the optional graph-store direction because it is Apache-2.0 and fits
PostgreSQL-oriented enterprise teams.

