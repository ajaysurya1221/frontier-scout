# ADR 0001: Python Typed Monolith

Use Python 3.11 for the release slice. It keeps the existing CLI and lab
investment intact while enabling strict type checks for the new platform
modules. TypeScript can be added later for UI or MCP clients.

