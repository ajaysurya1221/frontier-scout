# ADR 0005: Local ReBAC

Use an OpenFGA-inspired local ReBAC evaluator for v0.1. It proves the model and
test semantics without requiring a server. Production deployments can swap in
OpenFGA or SpiceDB.

